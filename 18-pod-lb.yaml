apiVersion: v1
kind: Pod
metadata:
  name: Service-load-demo
spec:
  labels:
    project:  roboshop
    component:  fronend
    environment:  dev
    purpose:  project-LoadBalancer
  containers:
  - name: nginx
    image:  nginx
